# -*- coding: utf-8 -*-
"""
===================================
任务取消信号
===================================

统一的「用户已请求取消」信号，供 task_queue / pipeline / analyzer / LLM 后端
各层共用，避免循环 import（本模块不依赖任何业务层代码）。
"""


class TaskCancelledError(Exception):
    """协作式取消信号：任意阶段检测到取消请求后抛出，逐层向上传播直到任务执行入口捕获。"""
